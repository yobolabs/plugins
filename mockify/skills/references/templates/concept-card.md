# Concept Card — <id>: <direction name>

> Director dispatch: **concept mode**. Seed: <the distinct direction hypothesis this dispatch was given>.
> Pack emphasis: <which lenses this bet leans on hardest>.
> Field order below is load-bearing — the judge scores cards in this order, and the human compares them side by side. Every bet cites a recon fact by tag.

## 1. Direction seed served
<The seed as dispatched, and how this card answers it — restated so drift from the seed is visible. A card that quietly swapped its seed is a card the judge scores down.>

## 2. IA / layout bet
<The structural wager: information architecture, navigation shape, layout grammar (dashboard / form / list / split-pane), density stance. Specific enough that two screens built from it would look like siblings. Names the real components it leans on.>

## 3. Key-screen rationale
<Which 1–2 screens were rendered as proof, and why THOSE — the screens where this direction either earns the persona's job or fails it. What the mockups demonstrate that prose can't.>

## 4. Mockup file pointers
| Screen | File |
|---|---|
| <route / screen name> | `mockups/<slug>-concept-<letter>-<screen>.html` |
| <route / screen name> | `mockups/<slug>-concept-<letter>-<screen>.html` |

Self-contained HTML, M-rule-conformant, published as an Artifact for clickthrough at the human gate.

## 5. Why it's usable for this persona
<The causal claim: what in this IA/layout lets THIS persona complete the job-to-be-done — lower scan cost, fewer steps, faster recovery. Cite the recon facts it stands on (`checked`/`reasoned` tags). A direction usable by "anyone" was designed for no one.>

## 6. Design-system fit argument
<Why this direction lives INSIDE the design system instead of forking it: which existing components and token roles carry it, where it stretches them, and why each stretch is a variant, not an invention. Anything needing a NEW component is named here and lands in the spec's new-component requests — never smuggled in.>

## 7. Biggest risk + early sign
<The most likely way this direction fails — named honestly, with the observable early sign it's failing (at crit, at the judge gate, or in the built screens). A card with no real risk is a card that wasn't thought through.>
