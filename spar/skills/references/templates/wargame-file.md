# War-Game — <mission-name>

> Executor: <human — name>. Depth walked: order <N>. Domain: <domain, pack status>. Status: <draft-1 | judged-accept>.

## 1. Epistemic base

The facts this war-game stands on. Every entry tagged `verified` (cited, dated) / `inferred` (chain) / `assumed` (ledger ref) / `unknown → hunted → outcome`. Web claims graded A/B/C.

| # | Fact | Tag | Grade | Source (dated) |
|---|------|-----|-------|----------------|

## 2. Actor/incentive map (summary)

Per actor: wants / controls / exposed-to / likely reaction. Causal edges for non-actor mechanics. Off-table actors included.

## 3. Move tree

Numbered moves; branches lettered (4a, 4b). One line each here; full triples below. Moves are decisions/actions in the world — sign, counter-offer, launch, hire, walk away. HUMAN GATE moves (from the standing-rules sweep) marked as such.

## 4. Moves

### Move <n> — <name>

- **Action:** <literal — what the human says, sends, signs, or does. No "as discussed".>
- **Expected observation (success):** <exactly what the human sees/hears/counts — the reply, the number, the signed doc. Not "it goes well.">
- **Expected observation (failure):** <what they observe instead; if identical to success (e.g. silence), the discriminating probe and its deadline>
- **Most-likely reaction:** <from the actor whose payoff drops most — cause → observable signals. Pack/base-rate refs where they apply — those are guaranteed, not hypothetical.>
- **Countermove (pessimistic):** <the failure is structural — recovery path; does the plan fork?>
- **Countermove (optimistic):** <the failure is superficial — cheap retry/fix + how to confirm it was superficial rather than masked>
- **Fork triggers:** <observe X → route A; observe Y → route B — observables defined BEFORE acting>
- **Consequences:** <order-numbered refs into §5>

## 5. Consequence tree (cascade)

Order-numbered findings per move, each with the map edge it rode (actor exposure or causal edge), landing time (immediate / weeks / next cycle), severity (kills the decision / degrades / cosmetic), and disposition (new move / fork trigger / abort condition / accepted risk).

## 6. Opportunity register (scout)

| Finding | Enabled by (move/order) | Better axes (+cost) | Consumers now→plausible | Partition | Trigger/Seam |
|---------|------------------------|---------------------|------------------------|-----------|--------------|

Partition values: now / designed-for (seam named) / later (trigger named + reversibility note) / rejected (why, one line).

## 7. Now/later partition (mission scope)

- **Now:** <…>
- **Designed-for:** <item — seam: …>
- **Later:** <item — promotes when: … — reversibility: two-way/one-way>
- **Out / rejected:** <…>

## 8. Assumptions & surviving unknowns

Ledger refs. Each: what would verify it, and which moves it underpins (blast radius if false).

## 9. Abort conditions

States where the human STOPS and reassesses (or escalates to advisors). Specific, observable, no judgment calls: <e.g. "counterparty silent > N days after countermove 3b", "total committed exposure exceeds $X", "any term appears in their draft that this war-game didn't enumerate", "you catch yourself renegotiating a conceded point">.

## 10. Mission success criteria

The final observations that mean done — the human verifies each before calling the mission complete.
